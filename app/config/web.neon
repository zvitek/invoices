common:
	parameters:

	application:
		errorPresenter: Web:Error

	services:
		- App\WebRouterFactory
		router: @App\WebRouterFactory::createRouter

production < common:
	parameters:

development_localhost < common:
	parameters:

development < common:
	parameters:
